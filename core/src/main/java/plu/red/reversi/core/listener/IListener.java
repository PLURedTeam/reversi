package plu.red.reversi.core.listener;

/**
 * Glory to the Red Team.
 *
 * Basic Listener Interface Stub. Does not perform any actions or do anything, but extended by all Listener interfaces
 * so that they may be grouped together as the same type. If an Object implements the IListener interface, it will not
 * be notified when any actions happen. Use extensions of the IListener interface for event signalling.
 */
public interface IListener {

    /* STUB */

}
